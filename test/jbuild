(jbuild_version 1)

(alias
 ((name    runtest)
  (deps    (test_main.exe))
  (action  (run ${<}))))

(executable
 ((name test_main)
  (modules (test_main yojson_server_test))
  (libraries (jsonrpc-ocaml jsonrpc-ocaml.yojson lwt lwt.unix oUnit))))
